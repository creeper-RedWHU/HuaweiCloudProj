---
id: solution-deploy-skill-main
name: solution-deploy-skill-main
description: 解决方案部署助手 - 支持三种部署模式（单机ECS/CCE容器化/高可用架构），自动复用资源、智能规格推荐、自动回滚。触发词：部署应用、华为云ECS、应用上线、CCE部署、高可用部署。
---

# 解决方案部署助手

一键部署当前项目到华为云，支持三种部署模式，自动分析、自动复用、自动部署

---

## ✨ 特性

- ✅ **开箱即用**：terraform-installer 已打包，无需额外安装依赖
- ✅ **网络友好**：使用华为云镜像，避免 GitHub 网络问题
- ✅ **版本一致**：自动管理 Provider 版本，确保一致性
- ✅ **自动安装**：检测到环境缺失时自动安装 Terraform 和 Provider
- ✅ **跨平台支持**：支持 Linux、macOS、Windows 三大平台
- ✅ **版本锁定**：Terraform 1.9.8 + Provider 1.90.0，确保稳定性

---

## 🔒 版本管理

本 Skill 使用固定版本，确保部署稳定性：

| 组件 | 版本 | 来源 | 说明 |
|------|------|------|------|
| **Terraform** | **1.9.8** | HashiCorp Releases | 稳定版本，不自动升级 |
| **HuaweiCloud Provider** | **1.90.0** | 华为云镜像 | 与 Terraform 1.9.8 兼容 |

### 版本兼容性

| Terraform | Provider | 兼容性 |
|-----------|----------|--------|
| 1.9.8 | 1.90.0 | ✅ 已验证 |
| 1.9.8 | 1.87.0 | ✅ 兼容 |
| 1.15.x | 1.90.0 | ⚠️ 待验证 |

### 版本锁定策略

**为什么锁定版本？**
- ✅ 避免新版本的潜在问题
- ✅ 确保部署结果可重复
- ✅ 与现有环境兼容
- ✅ 降低维护成本

**如何升级版本？**
1. 在测试环境验证新版本
2. 确认兼容性后更新配置
3. 更新 `required_version` 和 `version`
4. 重新部署并验证

---

## 🖥️ 平台支持

本 Skill 支持以下平台：

| 平台 | 安装方式 | 说明 |
|------|----------|------|
| **Linux** (Debian/Ubuntu) | APT → Binary | 优先 APT，失败则二进制 |
| **Linux** (RHEL/CentOS) | Binary | 直接二进制下载 |
| **macOS** | Binary | 直接二进制下载 |
| **Windows** | Binary | 直接二进制下载 |

### Windows 特殊说明

**安装方式**：
- 默认使用二进制下载（最快最可靠）
- 可选包管理器：Scoop（用户级）、Chocolatey（系统级）

**安装目录**：
- 管理员：`C:\Program Files\Terraform\`
- 普通用户：`%LOCALAPPDATA%\Terraform\`

**PATH 配置**：
- 脚本自动添加到 PATH
- **需要重新打开终端**使 PATH 生效

**Provider 安装**：
- 使用华为云镜像（避免 GitHub 超时）
- 配置 `terraform.rc` 指向本地目录

---

## 步骤 0 执行规范（强制）

**AI必须严格遵守以下规范，不得违反！**

---

### 🔒 强制检查点（AI执行任何操作前必须验证）

**AI执行任何部署操作前，必须先执行环境检测脚本并验证返回码：**

```bash
python bundled_resources/scripts/terraform-installer/scripts/prepare_environment.py --check
# 返回码 0 = 环境就绪，可继续
# 返回码 1 = 环境未就绪，必须先执行步骤0
```

**强制规则**：
- ✅ 返回码为0时，才可继续执行步骤1（项目分析）
- ❌ 返回码非0时，**禁止继续执行任何部署相关操作**
- ❌ 禁止跳过此检查直接进入项目分析
- ❌ 禁止假设环境已就绪而不执行检查

**AI内部状态检查**：
```python
# AI必须在执行任何操作前维护此状态
env_check_passed = False  # 初始为 False

# 执行检查后更新
result = subprocess.run(["python", "prepare_environment.py", "--check"])
env_check_passed = (result.returncode == 0)

# 只有 env_check_passed == True 时才能继续
if not env_check_passed:
    # 必须执行步骤0：环境初始化
    # 禁止跳转到步骤1
```

---

### 执行方式

**调用 terraform-installer 子 Skill**：`./bundled_resources/scripts/terraform-installer/SKILL.md`

```bash
# 一键环境准备（推荐）
python bundled_resources/scripts/terraform-installer/scripts/prepare_environment.py

# 指定 tf 文件（自动同步版本）
python bundled_resources/scripts/terraform-installer/scripts/prepare_environment.py main.tf
```

**执行流程**：
```
[1/4] 检测 Terraform
[2/4] 检测 Provider
      └─ 未安装 → 自动安装
[3/4] 同步 Provider 版本
[4/4] 验证版本一致性
```

---

### 失败处理

**任何步骤失败，AI必须**：

1. ❌ 立即停止，不继续后续步骤
2. 📢 明确告知用户失败原因
3. 💡 提供具体修复建议
4. 🔄 等待用户修复后重新执行

---

### 成功标准

| 检查项 | 成功标准 |
|--------|----------|
| Terraform | 已安装且可执行 |
| Provider | 已安装且版本正确 |
| 版本一致性 | tf 文件与本地版本一致 |

---

### 禁止行为

- ❌ 跳过环境检测直接部署
- ❌ 使用硬编码的 Provider 版本
- ❌ 使用范围版本（如 `>= 1.87.0`）
- ❌ 版本不一致时继续部署
- ❌ 在环境未就绪时尝试部署

---

### 版本规范

✅ **正确示例**：
```hcl
required_providers {
  huaweicloud = {
    source  = "huaweicloud/huaweicloud"
    version = "1.90.0"  # 精确版本，与本地一致
  }
}
```

❌ **错误示例**：
```hcl
required_providers {
  huaweicloud = {
    source  = "huaweicloud/huaweicloud"
    version = ">= 1.87.0"  # 范围版本，可能下载新版本
  }
}
```

---

## 三种部署模式

| 模式           | 适用场景           | 核心组件           |
| ---------------- | -------------------- | -------------------- |
| **单机ECS**    | 小型应用、测试环境 | 1台ECS + EIP       |
| **CCE容器化**  | K8s原生应用        | CCE集群 + Node     |
| **高可用架构** | 生产环境           | 2台ECS + ELB + RDS |

---

## 模式1：单机ECS

**AI执行前必须读取：** `bundled_resources/references/single-ecs-deploy-flow-v2.md`

**Provider由 terraform init 自动从 Terraform Registry 下载，无需手动安装！**

### 一键部署命令

```bash
bash <skill路径>/bundled_resources/scripts/deploy_single_ecs_deterministic.sh
```

### 清理资源命令

```bash
export HW_ACCESS_KEY=<用户的AK>
export HW_SECRET_KEY=<用户的SK>
bash <skill路径>/bundled_resources/scripts/cleanup_standard.sh <项目名称> <区域>
```

### 预期耗时

| 阶段       | 耗时        |
| ------------ | ------------- |
| 环境初始化 | 10-15秒     |
| 收集配置   | 用户输入    |
| 执行部署   | 2-3分钟     |
| **总计**   | **约3分钟** |

---

## 模式2：CCE容器化

**架构图和费用明细：** 动态从 `references/spec-cce.md` 读取

**AI展示模式信息时，必须执行：**

```bash
python bundled_resources/scripts/parse_deploy_specs.py
```

然后从输出中提取 `cce` 模式的 `architecture`（架构图）和 `resources`（费用明细）展示给用户。

**镜像准备要求：**

CCE模式需要容器镜像，部署前请确保：

1. 已构建容器镜像并推送到镜像仓库（SWR/Docker Hub）
2. 镜像地址格式：`swr.cn-north-4.myhuaweicloud.com/{org}/{image}:{tag}` 或 `{user}/{image}:{tag}`
3. 详细说明请参考：`references/cce-image-build-guide.md`

**适用场景：** Kubernetes原生应用、微服务架构、需要容器编排能力、未来需要弹性伸缩

---

## 模式3：高可用架构

**架构图和费用明细：** 动态从 `references/spec-ha-ecs-elb-rds.md` 读取

**AI展示模式信息时，必须执行：**

```bash
python bundled_resources/scripts/parse_deploy_specs.py
```

然后从输出中提取 `ha` 模式的 `architecture`（架构图）和 `resources`（费用明细）展示给用户。

**适用场景：** 生产环境、企业级应用、需要高可用性保障、需要数据库持久化、对SLA有要求的场景

---

## 快速开始

**一句话触发部署：**

```
部署应用                  # AI自动推荐模式
使用单机ECS模式部署      # 强制使用单机ECS模式
使用CCE容器化模式部署    # 强制使用CCE模式
使用高可用架构部署       # 强制使用高可用模式
```

**AI将自动执行：** 1. 分析项目 → 2. 收集配置 → 3. 自动部署 → 4. 输出结果

**常用指令：**

| 指令                    | 功能                               |
| ------------------------- | ------------------------------------ |
| `部署应用`              | 一键部署（推荐，3步完成）          |
| `快速部署`              | 单机ECS一键部署（仅输入3个必填项） |
| `使用单机ECS模式部署`   | 强制使用单机ECS模式                |
| `使用CCE容器化模式部署` | 强制使用CCE模式                    |
| `使用高可用架构部署`    | 强制使用高可用模式                 |
| `查看我的应用`          | 查看所有运行中的项目               |
| `清理资源`              | 列举资源并确认后执行 terraform destroy |

---

## 首次使用准备

### 必需准备

| 准备项     | 获取方式                                           | 说明                         |
| ------------ | ---------------------------------------------------- | ------------------------------ |
| 华为云账号 | https://console.huaweicloud.com/                   | 需完成实名认证               |
| Access Key | https://console.huaweicloud.com/iam/#/accessKey    | 提前创建并保存，SK仅显示一次 |

### 可选准备

- **SSH密钥对** - 用于免密登录ECS
- **自定义域名** - 用于绑定EIP访问

### 预计费用（参考）

> 实际费用请以部署时AI展示的为准。AI会调用 `parse_deploy_specs.py` 从spec文件读取最新费用。

| 规格  | 按需费用（参考） | 包月费用（参考） | 适用场景         |
| ------- | ------------------ | ------------------ | ------------------ |
| 1核2G | ~¥0.10/小时      | ~¥50/月          | 小型应用/测试    |
| 2核4G | ~¥0.16/小时      | ~¥80/月          | 中型应用（推荐） |
| 4核8G | ~¥0.32/小时      | ~¥160/月         | 大型应用         |

### 部署时间预估

| 阶段       | 预计耗时    |
| ------------ | ------------- |
| 项目分析   | 10-30秒     |
| 账号验证   | 5-10秒      |
| 创建ECS    | 2-3分钟     |
| 初始化环境 | 2-3分钟     |
| 部署应用   | 30秒-1分钟  |
| **总计**   | **5-8分钟** |

---

## 常见错误速查表

| 错误                | 原因                | 解决方案                                                  |
| --------------------- | --------------------- | ----------------------------------------------------------- |
| AK/SK无效           | 密钥错误或已失效    | 重新创建：https://console.huaweicloud.com/iam/#/accessKey |
| 区域资源不足        | 所选区域无可用资源  | 自动切换区域或降级规格                                    |
| 端口访问失败        | 安全组未开放        | 检查安全组规则，确保开放22/80/443端口                    |
| SSH连接失败         | ECS未就绪或密码错误 | 等待2-3分钟后重试，确认密码正确                           |
| Terraform初始化超时 | GitHub不可达        | 自动跳过校验，使用国内镜像                                |
| 部署失败            | 资源创建异常        | 发送「清理资源」后重试                                    |

---

## 用户交互原则

**AI在整个部署过程中，必须遵循以下原则：**

### 信息展示原则

- 展示关键进度节点和耗时较长的操作
- 隐藏技术细节、调试过程、重试过程

### 用户应该看到的内容

```
⏳ [1/7] 配置Terraform环境...
✅ [1/7] 配置Terraform环境        3秒
⏳ [4/7] 创建ECS实例（约2-3分钟）...
✅ [4/7] 创建ECS实例（2核4G）   2分18秒
```

### 需要隐藏的技术细节（统一禁止列表）

- 任何"让我尝试..."、"让我检查..."、"等待..."、"诊断..."的表述
- 任何技术调试信息、内部判断过程、重试过程
- 任何镜像源切换/连接失败信息
- "GitHub下载超时"、"Terraform初始化成功！"、"检测到可复用资源"
- "State版本问题"、"plugin-dir问题"等terraform内部细节
- 任何terraform/Python SDK命令输出和安装过程
- Docker安装/镜像拉取过程和错误（CCE模式）

### 耗时操作展示方式

| 操作              | 预计耗时   | 展示方式                       |
| ------------------- | ------------ | -------------------------------- |
| 配置Terraform环境 | <10秒      | 只显示完成状态                 |
| 初始化Terraform   | 1-2分钟    | 显示"下载Provider，约1-2分钟"  |
| 创建ECS实例       | 2-3分钟    | 显示"约2-3分钟"                |
| 等待ECS就绪       | 2-3分钟    | 显示"等待ECS就绪（约2-3分钟）" |
| 上传应用代码      | 30秒-2分钟 | 显示"上传应用代码..."          |

### 后台命令执行规范

**所有耗时操作必须使用后台执行：**

| 操作类型           | 执行方式                                  | 用户可见  |
| -------------------- | ------------------------------------------- | ----------- |
| terraform init     | background=true + notify_on_complete=true | 不可见 |
| terraform apply    | background=true + notify_on_complete=true | 不可见 |
| ECS创建等待        | background=true + notify_on_complete=true | 不可见 |

- 轮询间隔至少15秒，禁止频繁轮询
- 优先使用 `notify_on_complete=true` 替代主动轮询
- 禁止使用 `sleep` + `bash-output` 循环检查进度

### 错误处理原则

- 技术问题 → 静默自动尝试所有解决方案
- 只有全部失败才向用户报告简洁的错误信息
- 错误信息必须简洁明了，包含解决方案

---

## 容器化部署自动处理（仅CCE模式）

> **单机ECS模式和高可用架构模式默认不使用容器，以下内容不适用。**

| 问题                     | 自动处理方案                                                                     |
| -------------------------- | ---------------------------------------------------------------------------------- |
| Docker安装失败           | 自动尝试多个国内镜像源（阿里云→腾讯云→中科大→清华）                              |
| Docker镜像拉取失败       | 自动配置多个镜像加速器                                                           |
| python:3.11-slim拉取失败 | 自动使用阿里云镜像：`registry.cn-hangzhou.aliyuncs.com/library/python:3.11-slim` |
| SSL连接重置              | 自动重试下一个镜像源                                                             |

---

## 执行流程强制约束

**AI必须严格按照以下顺序执行，每完成一步立即输出结果：**

```
用户发送「部署应用」
        ↓
   步骤0：环境检测（必须执行，返回码验证）
        ↓
   【返回码=0】环境就绪，继续
   【返回码≠0】执行环境初始化，禁止跳过
        ↓
   步骤1：项目分析 + 部署模式推荐
        ↓
   【等待确认】选择部署模式
        ↓
   步骤2：必填配置 + 账号状态检测
        ↓
   步骤3：自动部署
        ↓
   步骤4：部署结果
```

**禁止行为：**
- ❌ **禁止跳过步骤0**：必须先执行 `prepare_environment.py --check` 并验证返回码
- ❌ 禁止跳步、合并步骤、将结果延迟输出
- ❌ 禁止在用户确认前就开始收集配置信息
- ❌ 禁止省略步骤总览进度条
- ❌ 禁止假设环境已就绪而不执行检查

---

## 防跳步检查机制

**前置条件检查表：**

| 步骤              | 前置条件                       | 不满足时处理      |
| ------------------- | -------------------------------- | ------------------- |
| 步骤0：环境检测   | 用户发送「部署应用」           | 等待用户触发      |
| 步骤1：项目分析   | 步骤0已完成 + 返回码=0         | 返回步骤0执行初始化 |
| 步骤2：必填配置   | 步骤1已完成 + 用户确认部署模式 | 提示先完成步骤1   |
| 步骤3：自动部署   | 步骤2已完成 + AK/SK验证通过    | 返回步骤2重新输入 |
| 步骤4：部署结果   | 步骤3已完成 + 资源创建成功     | 自动回滚并重试    |

**步骤编号规范：**
- 编号必须连续递增：步骤0 → 步骤1 → 步骤2 → 步骤3 → 步骤4
- 步骤0是强制第一步，禁止跳过
- 子步骤使用 X.Y 格式（如步骤2.1），不显示在总览进度条中
- 步骤名称必须严格使用：环境检测、项目分析、必填配置、自动部署、部署结果

**AI内部维护部署状态（用户不可见）：**

```python
deployment_state = {
    "step0_completed": False,      # 环境检测完成
    "env_check_passed": False,     # 环境检测返回码=0
    "step1_completed": False,      # 项目分析完成
    "step2_completed": False,      # 必填配置完成
    "step2_mode": None,            # 选择的部署模式
    "step2_ak_valid": False,       # AK/SK验证通过
    "step3_completed": False,      # 自动部署完成
    "step4_completed": False,      # 部署结果输出
    "project_dir": None,           # 项目目录
    "resources_created": []        # 已创建资源列表
}
```

---

## 步骤总览进度条

**每次输出时必须展示：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 应用部署流程
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────────────────────┐
│  ✅⏳⏳⏳⏳⏳  1/5 步骤              │
│                                      │
│  ✅ 步骤0：环境检测    ← 已完成       │
│  ⏳ 步骤1：项目分析    ← 进行中       │
│  ⬜ 步骤2：必填配置                   │
│  ⬜ 步骤3：自动部署                   │
│  ⬜ 步骤4：部署结果                   │
└──────────────────────────────────────┘
```

- ✅ 已完成，⏳ 进行中，⬜ 待执行
- **步骤0是强制第一步**，必须在进度条中显示
- 禁止将子步骤显示在进度条中
- 步骤3内部子步骤（配置Terraform、创建ECS、上传代码等）不显示在总览中

---

## 步骤1：项目分析 + 部署模式推荐

**环境检测通过后，AI必须立即分析项目并输出完整报告，包含以下6个部分：**

### 1.1 项目基本信息

- 检测项目类型（Node.js/Python/Java/Go等）
- 分析项目依赖和运行环境需求
- 检测应用端口、数据库类型等关键配置

### 1.2 部署模式推荐

- 根据项目特征推荐部署模式：
  - **需要数据库** → 推荐高可用架构（含RDS）
  - **K8s原生应用** → 推荐CCE容器化
  - **其他情况** → 推荐单机ECS（默认）
- 检测可复用资源，节省费用

### 1.3 部署所需详细资源清单

**AI必须调用 `python parse_deploy_specs.py` 获取资源数据，并展示详细的资源清单表格：**

单机ECS模式示例：
```
📦 部署所需资源清单
┌────────────────────────────────────────────────────┐
│ 资源类型         │ 规格              │ 数量 │ 说明  │
├──────────────────┼───────────────────┼──────┼───────┤
│ 云服务器 ECS     │ s6.large.2 (2U4G) │ 1台  │ 应用服务器 │
│ 系统盘           │ 40GB GPSSD        │ 1块  │ 系统存储  │
│ 数据盘           │ 100GB GPSSD       │ 1块  │ 应用数据  │
│ 弹性公网IP EIP   │ 5Mbps BGP         │ 1个  │ 公网访问  │
│ 虚拟私有云 VPC   │ 192.168.0.0/16   │ 1个  │ 网络隔离  │
│ 子网 Subnet      │ 192.168.1.0/24   │ 1个  │ 子网划分  │
│ 安全组 SG        │ 入放通22/80/443/8080 │ 1个  │ 访问控制  │
└──────────────────┴───────────────────┴──────┴───────┘
```

高可用架构模式需额外展示：ELB、RDS、第二个可用区子网等资源。

### 1.4 费用明细

**AI必须调用 `python parse_deploy_specs.py` 获取费用数据，并展示逐项费用明细表格：**

```
💰 费用明细（推荐模式：单机ECS）
┌──────────────────────────────────────────────────────────────┐
│ 资源                        │ 月费用     │ 日费用    │ 按需计费    │
├─────────────────────────────┼────────────┼──────────┼─────────────┤
│ ECS s6.large.2 (2U4G)      │ ¥256.95   │ ¥8.57   │ ¥0.16/小时  │
│ 系统盘 40GB GPSSD          │ ¥32.80    │ ¥1.09   │ ¥0.02/小时  │
│ 数据盘 100GB GPSSD         │ ¥82.00    │ ¥2.73   │ ¥0.04/小时  │
│ 带宽 5Mbps                 │ ¥50.00    │ ¥1.67   │ ¥0.12/小时  │
├─────────────────────────────┼────────────┼──────────┼─────────────┤
│ 合计                       │ ¥421.75   │ ¥14.06  │ ¥0.34/小时  │
└─────────────────────────────┴────────────┴──────────┴─────────────┘
⚠️ 以上为参考价格，实际以华为云账单为准
```

**费用展示规范：**
1. 调用脚本获取实时费用：`python parse_deploy_specs.py`
2. 必须展示逐项费用明细表格（不能只展示合计）
3. 必须同时展示月费用、日费用和按需计费三种计费方式
4. 必须包含提示：`⚠️ 以上为参考价格，实际以华为云账单为准`

### 1.5 部署上线预估时间

**AI必须展示各阶段预估耗时和总耗时：**

```
⏱️ 部署上线预估时间
┌──────────────────────────────────────────┐
│ 阶段             │ 预计耗时    │ 说明    │
├──────────────────┼─────────────┼─────────┤
│ 项目分析         │ 10-30秒    │ 已完成  │
│ 账号验证         │ 5-10秒     │ AK/SK  │
│ 创建云资源       │ 2-3分钟    │ ECS等  │
│ 等待资源就绪     │ 2-3分钟    │ 初始化 │
│ 上传应用代码     │ 30秒-1分钟 │ 代码   │
│ 启动并验证服务   │ 30秒-1分钟 │ 健康检查│
├──────────────────┼─────────────┼─────────┤
│ 总计预估         │ 5-8分钟    │ -      │
└──────────────────┴─────────────┴─────────┘
```

**不同模式的时间预估：**

| 模式 | 创建资源 | 等待就绪 | 部署应用 | 总计 |
|------|---------|---------|---------|------|
| 单机ECS | 2-3分钟 | 2-3分钟 | 1-2分钟 | **5-8分钟** |
| CCE容器化 | 5-8分钟 | 3-5分钟 | 1-2分钟 | **9-15分钟** |
| 高可用架构 | 5-8分钟 | 5-8分钟 | 2-3分钟 | **12-19分钟** |

### 1.6 可复用资源检测

- 检测已有VPC/安全组/EIP等可复用资源
- 展示复用后可节省的费用

**步骤1完整输出示例：**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 项目分析报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 项目基本信息
  项目名称：pingpang-app
  项目类型：Python Flask Web应用
  依赖库：Flask 3.0.0、Flask-SQLAlchemy 3.1.1
  数据库：SQLite（本地文件）
  应用端口：5000

📌 推荐部署模式：单机ECS
  原因：小型Web应用，无外部数据库依赖，单机即可满足

📦 部署所需资源清单
  • ECS s6.large.2 (2U4G) × 1台
  • 系统盘 40GB GPSSD × 1块
  • 数据盘 100GB GPSSD × 1块
  • EIP 5Mbps BGP × 1个
  • VPC 192.168.0.0/16 × 1个
  • Subnet 192.168.1.0/24 × 1个
  • 安全组（放通22/80/443/8080）× 1个

💰 费用明细
  ECS s6.large.2      月费 ¥256.95  按需 ¥0.16/小时
  系统盘 40GB GPSSD   月费 ¥32.80   按需 ¥0.02/小时
  数据盘 100GB GPSSD  月费 ¥82.00   按需 ¥0.04/小时
  带宽 5Mbps          月费 ¥50.00   按需 ¥0.12/小时
  ─────────────────────────────────────
  合计                月费 ¥421.75（约¥14.06/天）  按需 ¥0.34/小时
  ⚠️ 以上为参考价格，实际以华为云账单为准

⏱️ 部署上线预估时间
  创建云资源    2-3分钟
  等待资源就绪  2-3分钟
  部署应用      1-2分钟
  ─────────────────────────
  总计预估      5-8分钟

🔄 可复用资源：未检测到可复用资源，将全部新建
```

**必须等待用户确认部署模式后才能进入步骤2**

---

## 步骤2：必填配置

**根据部署模式不同，需要收集的配置项不同：**

### 单机ECS模式必填项

1. 华为云 Access Key ID（获取：https://console.huaweicloud.com/iam/#/accessKey）
2. 华为云 Secret Access Key（创建AK时显示，仅显示一次）
3. ECS登录密码（8-26位，含大小写字母+数字+特殊字符）

### CCE容器化模式必填项

1. 华为云 Access Key ID
2. 华为云 Secret Access Key
3. 容器镜像地址（可选，默认nginx:latest）

### 高可用架构模式必填项

1. 华为云 Access Key ID
2. 华为云 Secret Access Key
3. ECS登录密码（8-26位，含大小写字母+数字+特殊字符）
4. RDS数据库密码（8-32位，含大小写字母+数字+特殊字符，MySQL root账户密码）

**安全说明：** AK/SK仅在内存中使用，不落盘存储

---

### 账号状态检测（步骤2的子步骤）

**AK/SK验证必须使用 terraform plan（唯一正确方式）：**

```bash
# AI内部执行（用户不可见，后台执行）
export HW_ACCESS_KEY="用户输入的AK"
export HW_SECRET_KEY="***"
terraform plan -var="access_key=$HW_ACCESS_KEY" -var="secret_key=$HW_SECRET_KEY"
# plan成功 → AK/SK有效  |  plan失败 → AK/SK无效，终止流程
```

**绝对禁止：** 手动计算HMAC签名、curl直接调用IAM API、Python HMAC签名、任何SDK签名计算

**验证失败时：** 立即终止 → 简洁提示错误+解决方案 → 等待重新输入

**检测失败输出示例：**

**AK/SK无效：**
```
❌ AK/SK验证失败
可能原因：AK/SK输入错误或已失效
解决：登录 https://console.huaweicloud.com/iam/#/accessKey 检查AK状态
```

---

## 步骤3：自动部署

**根据部署模式不同，执行的操作不同：**

| 部署模式   | 步骤3内部子步骤                                                                           |
| ------------ | ------------------------------------------------------------------------------------------- |
| 单机ECS    | 配置Terraform → 创建ECS → 等待就绪 → 上传代码 → 启动应用 → 验证服务                       |
| CCE容器化  | 配置Terraform → 创建CCE集群 → 创建Node → 部署Workload → 配置Service → 验证服务            |
| 高可用架构 | 配置Terraform → 创建VPC/子网 → 创建RDS → 创建ELB → 创建ECS(2台) → 配置健康检查 → 验证服务 |

**部署标准化约束：**

1. 使用标准部署脚本，禁止编写临时脚本
2. 固定流程，禁止反复尝试、检查环境、sleep循环
3. 遇到问题直接报错，不反复尝试

**单机ECS模式标准架构：**
- Flask/Gunicorn 直接运行（不使用Docker Compose，容器化属于CCE模式）
- Nginx 可选反向代理
- 固定流程：Terraform创建ECS（2-3分钟）→ 等待就绪（最多5分钟）→ 上传代码（30秒）→ 启动应用（30秒）

---

## 步骤4：部署结果

**必须包含：** 访问信息、部署详情、资源清单、健康检查、费用信息、后续操作

**资源ID必须完整输出，便于后续管理和清理。**

**单机ECS模式结果示例：**
```
🎉 部署成功

🌐 访问信息
  应用地址：http://123.45.67.89
  SSH连接：ssh root@123.45.67.89

💰 费用信息（月合计：¥421.75，按需：¥0.58/小时）
⚠️ 实际费用以华为云账单为准

📦 资源清单
  • VPC / 子网 / 安全组 / ECS / 系统盘 / 数据盘 / EIP（含ID）

🔍 健康检查
  ✅ HTTP状态：200 | 应用状态：运行中

💡 后续：查看日志 / 清理资源 / SSH连接
```

**部署失败 → 自动回滚 + 智能重试：**
- 第1次失败：自动切换同规格其他可用区域
- 第2次失败：自动降级规格
- 第3次失败：提示用户手动调整

---

## 部署后验证

### 单机ECS模式

```bash
ssh root@<EIP>
ps aux | grep -E "python|node|java"    # 查看应用进程
tail -f /opt/app/logs/app.log          # 查看应用日志
systemctl status nginx                 # 查看Nginx状态
```

### CCE容器化模式

```bash
kubectl config set-cluster cce --server=https://<EIP>:5443
kubectl get pods                       # 查看Pod状态
kubectl get svc                        # 查看Service
```

### 高可用架构模式

```bash
ssh root@<EIP>                         # 通过ELB
mysql -h <RDS_PRIVATE_IP> -u root -p   # 测试数据库连接
```

### 验证清单

```
□ 浏览器可正常访问应用地址
□ SSH可正常登录（单机ECS/高可用模式）
□ 服务运行正常，应用日志无错误
□ 业务功能正常
```

---

## 资源清理

**使用 Terraform destroy 进行安全清理，清理前必须列举资源并确认！**

### 标准清理流程

**步骤1：列举待清理资源**

```bash
cd <项目目录>/terraform
terraform state list
```

**步骤2：向用户确认**

将资源列表展示给用户，询问：
```
以下资源将被删除：
- huaweicloud_compute_instance.ecs
- huaweicloud_vpc_subnet.subnet
- huaweicloud_vpc.vpc
- ...

确认执行 terraform destroy？(y/n)
```

**步骤3：执行清理**

用户确认后执行：
```bash
terraform destroy -auto-approve
```

### AI 行为约束

**当用户发送「清理资源」时，AI必须：**

1. **先列举资源**：执行 `terraform state list`，展示待删除资源
2. **等待确认**：向用户展示资源列表，等待确认
3. **执行销毁**：用户确认后执行 `terraform destroy`
4. **输出结果**：`✅ 清理完成，已删除 N 个资源`

**禁止行为：**
- 禁止不经确认直接执行 destroy
- 禁止使用其他方式清理（手动删除、Python脚本等）
- 禁止展示技术细节（state文件内容、调试信息）

**违反此约束 = 严重错误！**

### 清理失败时的手动方式

1. 登录华为云控制台 https://console.huaweicloud.com/
2. 按顺序删除：ECS → EIP → 安全组 → 子网 → VPC
3. 确认资源已完全删除

---

## 环境初始化

**AI执行时，首先运行环境初始化脚本：**

```bash
bash bundled_resources/scripts/init_environment.sh
```

| 组件            | 版本          | 来源                         |
| ----------------- | --------------- | ----------------------------- |
| Terraform二进制 | 1.9.5         | HashiCorp Releases 动态下载  |
| 华为云Provider  | 由 required_providers 声明 | Terraform Registry 自动下载 |
| 华为云SDK       | latest        | pip动态安装                   |

**优势：** 遵循HashiCorp官方最佳实践，Provider版本由main.tf统一管理，无需维护预置二进制文件

---

## 回滚与撤销

### 部署失败自动回滚

```
❌ 部署失败 → 🔄 已自动回滚，无资源残留 → 💡 切换规格或区域后重试
```

### 部署成功后撤销

发送「清理资源」，AI会先列举待删除资源，确认后执行 `terraform destroy`。

### 重新部署

```bash
ssh root@<EIP>
cd /opt/app
# 编辑配置后重启
./start.sh
```

**安全说明：** 清理资源会删除ECS和数据盘，数据不可恢复；复用资源不会被删除；建议清理前备份重要数据

---

## 智能自动化规则

| 操作     | AI自动处理                          |
| ---------- | ------------------------------------- |
| 项目分析 | 自动检测类型、大小、生成部署配置 |
| 模式推荐 | 根据项目特征自动推荐部署模式     |
| 规格推荐 | 根据项目大小自动推荐最优规格     |
| 区域选择 | 默认华北-北京四                  |
| 计费方式 | 默认按需付费                     |
| 资源复用 | 自动检测并复用VPC/安全组/EIP     |
| 部署失败 | 自动回滚 + 自动重试其他规格      |
| 服务验证 | 自动等待HTTP服务就绪             |

---

## 资源复用规则

| 资源类型 | 自动处理                 |
| ---------- | -------------------------- |
| VPC      | 有则复用，无则新建       |
| Subnet   | 有则复用，无则新建       |
| 安全组   | 有则复用，无则新建       |
| EIP      | 有未绑定则复用，无则新建 |
| ECS      | 始终新建                 |
| 数据盘   | 始终新建                 |

---

## 常见问题自动处理

**AI遇到以下问题时，必须静默自动处理，禁止向用户展示任何调试/重试过程**

| 问题             | 自动处理                  |
| ------------------ | --------------------------- |
| Provider下载超时 | 静默切换华为云镜像         |
| ECS规格不可用    | 静默查询可用规格并重试     |
| 应用无法访问     | 静默排查安全组端口         |
| 复用资源冲突     | 静默切换新建模式           |
| SSH连接失败      | 静默等待ECS就绪后重试      |
| 应用环境初始化慢 | 静默等待就绪标记文件       |

---

## 前置条件

| 工具        | 要求       |
| ------------- | ------------ |
| Terraform   | >= 1.0.0   |
| 华为云AK/SK | 运行时输入 |

---

## 安全说明

- **密码安全**：ECS密码由用户设置，无默认值
- **密钥安全**：AK/SK仅内存使用，不落盘存储
- **敏感信息清理**：部署完成后自动清理terraform.tfvars中的明文密钥
- **资源隔离**：状态文件存储在项目目录 `.terraform/`
- **清理脚本安全**：从环境变量读取AK/SK，禁止硬编码

```python
# ✅ 正确：从环境变量读取
ak = os.environ.get('HW_ACCESS_KEY', '')
sk = os.environ.get('HW_SECRET_KEY', '')

# ❌ 禁止：硬编码
AK = 'your_ak_here'
```

---

## Terraform变量说明

### 公共变量（所有模式通用）

| 变量名          | 类型        | 默认值                | 说明                             |
| ----------------- | ------------- | ----------------------- | ---------------------------------- |
| `region`        | string      | cn-north-4            | 华为云区域                       |
| `project_name`  | string      | my-app                | 项目名称（用于资源命名）         |
| `environment`   | string      | dev                   | 环境标识（dev/test/prod）        |
| `primary_dns`   | string      | 100.125.1.250         | 主DNS服务器                      |
| `secondary_dns` | string      | 100.125.129.250       | 备DNS服务器                      |
| `common_tags`   | map(string) | {}                    | 自定义标签（自动合并到所有资源） |

### EIP配置变量

| 变量名            | 类型   | 默认值    | 说明                                      |
| ------------------- | -------- | ----------- | ------------------------------------------- |
| `eip_bandwidth`   | number | 5         | EIP带宽(Mbps)                             |
| `eip_type`        | string | 5_bgp     | EIP类型（5_bgp全动态BGP）                 |
| `eip_charge_mode` | string | bandwidth | 计费模式（bandwidth按带宽/traffic按流量） |

### 统一标签机制

**所有资源自动添加基础标签：** `project`、`deploy_mode`、`environment`、`managed_by`、`created_by`

自定义标签通过 `common_tags` 变量传入，自动合并。

---

## 资源目录

```
solution-deploy-skill/
├── SKILL.md                          # AI技能主文档
├── README.md                         # 使用指南
└── bundled_resources/
    ├── scripts/                      # 可执行脚本
    │   ├── deploy_single_ecs_deterministic.sh  # 单机ECS确定性部署
    │   ├── init_environment.sh       # 环境初始化
    │   ├── cleanup_standard.sh       # 资源清理（Shell入口）
    │   ├── cleanup_standard.py       # 资源清理（Python实现）
    │   ├── parse_deploy_specs.py     # 部署规格解析
    │   ├── verify_deployment.sh      # 部署后验证
    │   └── (Provider通过 terraform init 自动安装)
    ├── templates/terraform/          # Terraform模板
    │   ├── single-ecs/              # 单机ECS模板
    │   │   ├── main.tf              # 主配置（含变量定义）
    │   │   ├── terraform.tfvars     # 运行时配置
    │   │   ├── terraform.tfvars.example  # 配置示例
    │   │   └── user_data_single.sh  # ECS初始化脚本
    │   ├── cce/                     # CCE容器化模板
    │   │   ├── main.tf
    │   │   └── terraform.tfvars.example
    │   ├── ha/                      # 高可用架构模板
    │   │   ├── main.tf
    │   │   ├── terraform.tfvars.example
    │   │   └── user_data_ha.sh
    │   ├── modules/                  # 公共模块
    │   │   ├── network/             # VPC/子网模块
    │   │   ├── security_group/      # 安全组模块
    │   │   └── eip/                 # EIP模块
    │   └── terraformrc.example      # Terraform CLI配置示例
    ├── wheels/                       # Python包
    │   └── hcloud-2.20.0-py3-none-any.whl
    ├── demo/                         # 架构模板
    │   ├── single-ecs/
    │   ├── cce-container/
    │   ├── ha-architecture/
    │   └── matching_rules.md
    ├── references/                   # 参考文档
    │   ├── single-ecs-deploy-flow-v2.md  # 单机ECS标准流程
    │   ├── spec-single-ecs.md        # 单机ECS规格
    │   ├── spec-cce.md               # CCE规格
    │   ├── spec-ha-ecs-elb-rds.md    # 高可用规格
    │   ├── cce-image-build-guide.md  # CCE镜像构建指南
    │   ├── huaweicloud-errors.md     # 华为云错误码速查
    │   └── cost-calculation.md       # 费用计算说明
    └── bin/
        └── LICENSE.txt
```
